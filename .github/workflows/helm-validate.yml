name: Helm Validate

on:
  pull_request:
    paths:
      - "charts/**"
      - ".github/workflows/**"

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Helm
        uses: azure/setup-helm@v4
        with:
          version: v3.14.4

      - name: Helm lint
        run: |
          helm lint ./charts/repo/service

      - name: Helm template (smoke test)
        run: |
          helm template test ./charts/repo/service > /dev/null
